import { styled } from 'twin.macro'

export const Slider = styled.div`
   .slide {
      background: white;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      height: 400px;
      position: relative;
   }
`
